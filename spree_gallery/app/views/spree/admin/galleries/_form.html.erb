<%= form_for [:admin, @spree_gallery], :html => { :multipart => true } do |f| %>
  <% if @spree_gallery.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spree_gallery.errors.count, "error") %> prohibited this spree_gallery from being saved:</h2>

      <ul>
      <% @spree_gallery.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

     <%= f.field_container :title do %>
      <%= f.label :title %> <span class="required">*</span><br />
      <%= f.text_field :title, :class => 'fullwidth title' %>
      <%= f.error_message_on :title %>
    <% end %>

    <%= f.field_container :description do %>
      <%= f.label :description %><span class="required">*</span><br />
      <%= f.text_area :description, {:class => 'fullwidth'} %>
      <%= f.error_message_on :description %>
    <% end %>
    
    <%= f.field_container :pictures do %>
      <%= f.fields_for :pictures do |picture| -%>
        <%= picture.file_field :attachment %>
      <% end %>
    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
